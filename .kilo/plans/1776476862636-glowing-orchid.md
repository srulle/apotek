# Refaktorisasi ComboboxData.tsx

## Analisis Kondisi Saat Ini

File `combobox-data.tsx` saat ini **365 baris** dan memuat:

1.  Definisi tipe/interface (24-57)
2.  Logic utama component ComboboxData
3.  Handler logika pemilihan item
4.  Render item CommandItem beserta Popover didalamnya
5.  Render utama component
6.  ✅ Kode bagus: sudah menggunakan useMemo, useCallback dengan benar
7.  ❌ Masalah: Ada **concern campuran** (ui logic + business logic + default form content)

## Apakah Perlu Di Refaktor / Dipisahkan?

✅ **YA, perlu di pisahkan.**

---

## Alasan Refaktorisasi

1.  **Single Responsibility Principle** dilanggar:
    - Line 198-251: Ada **form default hardcode untuk Apotek** (Jumlah Beli, Harga Beli) yang bukan bagian dari generic combobox component
    - Ini membuat component tidak reusable untuk kasus lain
2.  Komponen terlalu besar (>350 baris)
3.  Logika render item + popover bisa diekstrak menjadi sub-komponen terpisah
4.  Ada 3 concern berbeda dalam 1 file:
    - Tipe data & interface
    - Logic state & handler
    - UI Rendering

---

## Rencana Struktur File Baru

### Struktur Final Sesuai Permintaan User

```
resources/js/components/combobox-data/
├── index.ts                # Export file utama
├── types.ts                # Semua interface / type definition
├── ComboboxData.tsx        # Komponen utama (tanpa default popover)
├── ComboboxItemRenderer.tsx # Sub komponen render item + popover trigger
└── presets/
    └── ApotekItemPopover.tsx # Default form apotek dipindahkan kesini
```

### Pilihan 2: Partial Split

Hanya pisahkan types dan default popover content saja, biarkan component utama tetap di 1 file.

---

## Langkah Refaktorisasi Detail

1.  Buat direktori `combobox-data/` dan sub-direktori `presets/`
2.  Buat `types.ts`: Pindahkan `ComboboxItem`, `ComboboxGroup`, `ComboboxDataProps` interface kesini
3.  Buat `presets/ApotekItemPopover.tsx`: Pindahkan seluruh hardcode form default (baris 198-251) kesini sebagai komponen terpisah
4.  Buat `ComboboxItemRenderer.tsx`: Pindahkan seluruh logika `renderCommandItem` + Popover wrapper kesini
5.  Buat `ComboboxData.tsx`: Komponen utama dengan logika state, handler, dan render utama saja (hapus form default dan logika render item)
6.  Buat `index.ts`: Setup export entry point
7.  **Backward Compatibility**: Pastikan semua props dan behaviour persis sama seperti sebelumnya, existing usage TIDAK PERLU dirubah apapun
8.  Hapus file asli `combobox-data.tsx` lama

---

## Estimasi Hasil

- File utama turun menjadi **~180 baris**
- Tiap file fokus hanya 1 tanggung jawab
- Component menjadi generic dan reusable
- Mudah untuk di test secara terpisah
- Default form apotek bisa di gunakan atau di ganti tanpa merubah core component

---

## Rekomendasi

Saya sarankan untuk menggunakan **Pilihan 1 (Full Split)** karena struktur ini akan jauh lebih mudah di maintain dan berskala ketika component ini nanti butuh penambahan fitur lain.
